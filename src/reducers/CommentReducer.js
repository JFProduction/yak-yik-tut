const initialState = {
    comments: [],
    fetching: false,
    fetched: false,
    error: null
}

const CommentReducer = (state=initialState, actions) => {
    return state
}

export default CommentReducer